# torch 2.7+ recommended for torch._grouped_mm (used by core/moe.py).
# NGC image nvcr.io/nvidia/pytorch:25.03-py3 ships torch 2.7.0a0 + CUDA 12.8.
# 25.01 ships torch 2.6.0a0 and has been observed to lack torch._grouped_mm.
# For local CPU dev / testing, torch >= 2.7 keeps behavior closest to H100 runs.
torch>=2.7.0,<2.9
# flash-attn 3 (Hopper-only, sm_90+) is required for production bench.
# Verified 2026-05-19: NGC pytorch images SHIP ONLY FA2 (flash_attn 2.7.x);
# both 25.03 and 25.06 confirmed. The earlier comment claiming NGC provides
# FA3 was an incorrect assumption. For FA3, two working paths exist:
#   (a) vanilla PyPI torch 2.9.1+cu128 + kernels==0.11.7 + varunneal/flash-attention-3
#       hub kernel (verified end-to-end). Recipe: auto/FA3_SETUP.md.
#   (b) build from source: flash-attention/hopper/setup.py install (~60 min CPU).
# NGC's custom torch ABI (nv25.03/nv25.06) does NOT match the prebuilt hub kernels.
# Performance reality at d=8/seq=2048: FA3-alone ≈ FA2 (Chinchilla 2× prediction
# was overstated; attention is not dominant at this scale).
# See dev/auto_findings/2026-05-20.md and auto/FA3_SETUP.md.
# HuggingFace `kernels` provides the hub-kernel loader used by core/flash_attention.py.
kernels
numpy
pandas
einx
einops
jaxtyping
pyarrow
psutil
requests
PyYAML
wandb
tokenizers
rustbpe
tiktoken
filelock
Jinja2
triton